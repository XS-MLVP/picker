#pragma once

#include <bits/stdc++.h>
#include "picker.hpp"

namespace picker {

namespace parser {
    int sv(picker::exports_opts &opts, std::vector<picker::sv_signal_define> &external_pin);
};
} // namespace pickerc